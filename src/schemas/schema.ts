import helloSchema from "./hello.schema";
import userSchema from "./user.schema";
import postSchema from "./post.schema";

const schemas = [userSchema, helloSchema, postSchema];

export default schemas;
